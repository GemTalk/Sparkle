# Run the Sparkle test suite. Sparkle must already be loaded into a Rowan-enable stone.

run
    | suite result |
    suite := Rowan projectTools test testSuiteForProjectsNamed: { 'Sparkle' }.
    result := suite run.
    result printString.
%
